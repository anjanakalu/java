```
# 1. Initialize a new Node.js project with default settings:
npm init -y

# 2. Set the project to use ES6 modules:
npm pkg set type="module"

# 3. Install Apollo Server and GraphQL:
npm install @apollo/server graphql

# 4. Install `nodemon` to automatically restart the server during development:
npm install --save-dev nodemon

```
